# Platform Core Assets

This folder contains build and configuration assets used by shared platform services.

It does not define runtime topology. Compose files under `compose/` define how services run and connect.
